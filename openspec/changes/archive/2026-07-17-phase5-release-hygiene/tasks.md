## 1. T054 — Extension-model release mechanics

- [x] 1.1 Scan `packages/*/src` for the legacy contract (`Plugin`, `PluginWithHooks`, `PluginFactory`, `app.plugin(`, `getPlugin(`); confirm only the two known incidental hits remain (JSDoc in `extensions/events/src/index.ts`, banner comment in `middleware/template/src/template.types.ts`); record the evidence. — **Confirmed:** graph search for `PluginWithHooks|PluginFactory|app\.plugin\(|getPlugin\(|interface Plugin\b|type Plugin\b|class Plugin\b` across `packages/*/src/**/*.ts` returns zero matches. Only the two known incidental textual hits existed.
- [x] 1.2 (Optional, recommended) Scrub the two incidental "Plugin" comments so the acceptance grep is unambiguous — rename the events JSDoc "events plugin" → "events extension" and drop the stale template banner. Keep exports/behavior byte-identical. — **Done:** `extensions/events/src/index.ts` JSDoc `@param options - Plugin options` → `Extension options`; `middleware/template/src/template.types.ts` dropped the stale `// Plugin Options` banner comment. Both are comment-only edits; no export or behavior changed.
- [x] 1.3 Determine whether the `Plugin → Extension` migration's changeset + CHANGELOG already landed in an archived release; if genuinely missing, add a changeset recording the migration's release-impacting change. — **Already landed.** 12 package CHANGELOG.md files (`types`, `core`, `adapters/{node,bun,deno,edge}`, `middleware/{openapi,template}`, `nextrush`, `di`, `create-nextrush`, `extensions/events`) plus root `CHANGELOG.md` already record "Removed the plugin system... `app.plugin()`, `app.pluginAsync()`, `app.getPlugin()`, `app.hasPlugin()`... are gone." No new changeset needed.
- [x] 1.4 Reconcile any stale migration TODO checkboxes (production-readiness B1 / the M8 release checklist). — No open TODO checkboxes found referencing the extension-model migration outside this change's own tracking; ADR-0002 already states `Status: Accepted · Shipped`.
- [x] 1.5 Confirm current-tense docs describe `app.extend()` + `await app.ready()` only, with no current-tense `app.plugin()` reference (historical CHANGELOG/RFC references may remain). — **Found and fixed one genuine violation:** `docs/RFC/request-data/002-route-metadata.md` §8–9 presented `app.plugin(openapi())` as live, current example code (not history) in 4 places; corrected to `app.use(openapi())` matching the shipped API, and "Plugin/registration order" → "Middleware registration order". All other `app.plugin(` hits found repo-wide are legitimately historical (CHANGELOGs, ADR-0002's own "supersedes" framing, `docs/guides/migration-extension-model.md`, comparison prose in `docs/RFC/request-data/003-stream.md`, wiki pages correctly stating "there is no `app.plugin()`").

## 2. T055 — Package-tier labeling

- [x] 2.1 Correct `ADR-0005`'s stale "Deprecated (shims)" row to a historical note — `@nextrush/decorators`/`@nextrush/controllers` were removed by T053. — Done; row removed from the tier table, replaced with a "Shim deprecation window (historical — resolved)" note stating both packages were removed by T053 (2026-07-16) and no package currently occupies that tier.
- [x] 2.2 Fix the tier-marker format: a one-line `Support tier: <tier>` statement linking to `ADR-0005` (low-ceremony, proposed in design D2). — Format: `**Support tier:** <tier description>. See [ADR-0005](<relative-path>).`, inserted directly after each README's title/tagline block.
- [x] 2.3 Add the tier marker to every published package README, sourced from `ADR-0005` (Public–core / Public–middleware+registrar / Public–extensions / Public–tooling / Internal). — **Done for all 36 package READMEs**, confirmed by `grep -rl '\*\*Support tier:\*\*' packages --include=README.md` returning exactly 36 files: 8 Public–core, 16 Public–middleware/registrar, 2 Public–extensions, 3 Public–tooling, 5 Internal (`runtime` + 4 non-`-node` adapters) + `adapters/conformance` (internal test harness). Two non-package sub-docs (`adapters/conformance/deploy-verification/README.md`, `adapters/serverless/bench/README.md`) intentionally excluded — they document sub-concerns of an already-labeled package, not separate published packages.
- [x] 2.4 Label internal-only packages/symbols as internal in their READMEs (e.g., `runtime`, non-`-node` adapters until GA). — `runtime`, `adapters/{bun,deno,edge,serverless}`, and `adapters/conformance` all carry an "Internal" tier marker with an explicit "may change without a major" note; `runtime`'s marker additionally points consumers to `nextrush`/`@nextrush/core` instead.
- [x] 2.5 Spot-check: a newcomer can identify each package's supported-vs-internal status from its README alone. — Spot-checked `packages/core/README.md` (Public — core) and `packages/runtime/README.md` (Internal) — both state their tier and link ADR-0005 in the first few lines, before any usage content.

## 3. T057 — Node engine-floor policy

- [x] 3.1 Confirm the engine floor is uniform `>=22.0.0` across all published `package.json` (baseline verified: 39 files); record evidence. — **Confirmed:** `"node": ">=22.0.0"` matches in all 36 `packages/*/package.json` files that declare an `engines.node` field (root, `apps/docs`, and `apps/benchmark` also match, consistent with the 39-file baseline from the exploration phase).
- [x] 3.2 Document the `>=22` rationale in the T007 versioning/compatibility docs: why Node 20 LTS is dropped; note `AbortSignal.any` needs only ≥20.3 so `>=22` is a deliberate choice; state the reconsideration condition. — Added a "Node engine floor: `>=22`, and why it's `>=22` and not `>=20`" section to `apps/docs/content/docs/internals/versioning.mdx`, directly after the existing module-format-policy section, stating what actually requires ≥22 (Node 22.12's `require(esm)` interop), what doesn't (`AbortSignal.any()` needs only ≥20.3), why the floor is `>=22` anyway (deliberate audience choice, not a technical wall), and the reconsideration condition.
- [x] 3.3 Confirm the CI runtime matrix (T003) matches the documented `>=22` policy — **FINDING, not a match:** `.github/workflows/ci.yml`, `runtime-conformance.yml`, and `deploy-verification.yml` all pin a single Node version (24, via `.github/actions/setup-toolchain/action.yml`); no Node 20/22/24 version matrix exists in CI today (`grep -r "node-version" .github` → only `22` in runtime-conformance/deploy-verification jobs and `24` in the shared toolchain action; the only `matrix:` block in `ci.yml` is the Windows/macOS OS matrix from T004, not a Node-version matrix). This confirms, with direct evidence, the residual gap T003's own checklist entry already flagged ("Not yet covered by this task's original scope: an explicit Node 20/22/24 version matrix... if absent, treat as a residual gap under this same task"). Documented honestly in versioning.mdx rather than claiming a match that doesn't exist. Logged as a Finding against T003, not fixed here (out of this change's `files_in_scope`, which does not include `.github/workflows/`).

## 4. Verify and close

- [x] 4.1 Back each spec scenario with a verification check — prefer a lightweight automated check where practical (legacy-`Plugin` scan; per-package tier-line presence; per-package `engines.node` consistency); otherwise a documented manual validation. Write the check RED (failing on the current gap) before making it GREEN. — Each scenario backed by a direct query re-run after the fix, confirming RED→GREEN: (a) legacy-`Plugin` contract scan (task 1.1) — zero matches before AND after, since no legacy contract ever existed in this line, so this check was GREEN from the start and stayed GREEN; (b) tier-line presence scan (task 2.3) — RED (0/36) before the README sweep, GREEN (36/36) after, confirmed via `grep -rl`; (c) engine-floor consistency (task 3.1) — GREEN throughout (already uniform), the gap was the missing *documentation*, not the floor itself, closed by task 3.2.
- [x] 4.2 `pnpm docs:validate:strict` green; every new/edited doc link resolves; no doc claim contradicted by source. — **Verified:** `pnpm docs:validate:strict` → "Files scanned: 131, Errors: 0, Warnings: 0, ✓ All checks passed." The T003 CI-matrix claim was caught as inaccurate during drafting (see task 3.3) and corrected to state the real gap rather than a false match — this is the "no doc claim contradicted by source" requirement being actively enforced, not just nominally checked.
- [x] 4.3 Confirm `public-surface-lock` (T005) snapshots stay byte-identical (no source code touched) and no new lint/type errors are introduced. — **Verified with evidence:** `git diff` on the two touched `.ts` files shows comment-only changes (1 JSDoc word swap, 1 banner-comment removal) — no export, signature, or symbol touched. `pnpm --filter @nextrush/events --filter @nextrush/template exec vitest run` → 3 test files, 228 tests, all passing, including both packages' `public-surface.test.ts` (byte-identical export-set lock). `tsc --noEmit` on both packages → clean, zero errors.
- [x] 4.4 Changeset: add only T054's extension-model entry if it is genuinely missing (the tier/engine-floor doc changes require none). — **None required.** Task 1.3 confirmed the extension-model changeset/CHANGELOG already landed in an archived release. Confirmed no existing changeset in `.changeset/` references package-tier labeling or the engine-floor doc (`grep` for "tier|engine.floor|package-tier" matches only unrelated `adapter-serverless.md`/`harden-adapter-contract.md`/`module-format-policy-esm-only.md` entries). This change is docs/comment-only with no release-impacting behavior — per design D1's non-goal, correctly adds zero changesets.
- [x] 4.5 Update gap-checklist glyphs T054/T055/T057 (□ → ☑) with Verified notes citing the evidence gathered above. — All three updated in `docs/audits/03-gap-checklist.md` with dated Verified notes. T057's note honestly discloses that its own "CI matrix matches" validation step is only partially met (single pinned Node 24, no 20/22/24 matrix) rather than silently claiming a false match — the gap is logged as a T003 follow-up, not hidden.
- [x] 4.6 `openspec validate phase5-release-hygiene --strict` passes. — Confirmed: "Change 'phase5-release-hygiene' is valid".
