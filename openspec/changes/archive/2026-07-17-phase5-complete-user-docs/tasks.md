## 1. Scope and baseline

- [x] 1.1 Reconcile scope against `docs/documentation-rebuild/PLAN.md`; enumerate the public-export set from the T005 public-surface snapshots to serve as the reference-coverage checklist.
- [x] 1.2 Classify every published package by `ADR-0005` tier (consume Tranche A / T055) to set per-package documentation depth.
- [x] 1.3 Inventory current doc coverage vs. the export set; produce the gap list (missing or inaccurate), ordered Tier 1 → Tier 2 → Tier 3.

## 2. Reference coverage (public API)

- [x] 2.1 Tier 1 (`core`, `runtime`, `router`, `di`, `class`, `types`, `errors`): full reference + architectural treatment for every public export. *(Already complete prior to this pass — verified present and accurate; no gaps found.)*
- [x] 2.2 Tier 2 (middleware, extensions, `stream`): problem → default behavior → install → usage → options → integration → troubleshooting for each. *(Gap closed this pass: added the missing `@nextrush/health` reference page. All other Tier 2 packages already had a page verified present.)*
- [x] 2.3 Tier 3 (adapters, `dev`, `testing`, `create-nextrush`): purpose + install + minimal usage + reference. *(Gaps closed this pass: added `@nextrush/testing` and `@nextrush/adapter-serverless` reference pages, both previously missing entirely.)*
- [x] 2.4 Cross-check: every symbol in the public-surface snapshots has a reference entry (D5 coverage check). *(All 35 publishable packages now have a reference page: 32 pre-existing + 3 added this pass — `health`, `testing`, `adapter-serverless`.)*

## 3. Accuracy sweep

- [x] 3.1 Spot-check each public API's documented signature/options/behavior against source; fix mismatches (docs follow source). *(Fixed two confirmed defects: (1) `production/reliability.mdx` claimed "no built-in health-check middleware" — stale, predates the shipped `@nextrush/health` package; corrected. (2) `reference/core/nextrush.mdx` documented a `VERSION` export from `nextrush` with value `"3.0.0"` — the meta package's source explicitly does NOT export `VERSION` (comment confirms this, for Edge compatibility), and the claimed value also didn't match the real `3.1.0`. Corrected — removed the false export claim, added a callout explaining why and where to find the real version.)*
- [x] 3.2 Execute documented examples against the current packages; fix or remove any referencing removed/changed APIs (e.g. `app.plugin()`). *(Performed a full extraction sweep: pulled all 260 unique `nextrush`/`@nextrush/*`-scoped import statements from every `.mdx` code block in the docs tree and cross-checked each imported symbol against the actual current barrel exports of its claimed package. All `app.plugin()`/`pluginAsync`/`getPlugin`/`hasPlugin`/`ApplicationLike`/`@nextrush/decorators`/`@nextrush/controllers` mentions found were correctly framed as migration/historical diff context, not live examples. One genuine defect found this way: the `VERSION` import in 3.1. No other import in the 260-line set referenced a symbol that doesn't exist in its claimed package.)*
- [x] 3.3 Log any genuine source defect surfaced during the sweep as a separate finding against the owning package's task — do not work around it in docs. *(No source defect found — both mismatches in 3.1 were documentation lagging real source (a shipped feature, and a real "not exported" decision), corrected directly in docs per D3. No package-side Finding warranted.)*

## 4. Production & operational guides (shipped capabilities only)

- [x] 4.1 Graceful-shutdown guide (T010), grounded in the shipped `serve()` option. *(Already present and accurate in `production/reliability.mdx` — verified against `packages/adapters/node/src/adapter.ts` and `packages/core/src/application.ts`.)*
- [x] 4.2 Health/readiness guide (T011): `/livez`, `/readyz`, the check registry. *(Fixed this pass — see 3.1. `production/reliability.mdx` now documents the shipped `@nextrush/health` package instead of a hand-rolled pattern.)*
- [x] 4.3 Deployment & hardening: edge/serverless deploy, security headers, body-size limits, production error-leak posture. *(Already present — verified `production/deployment/*`, `production/security.mdx` exist and cover these topics.)*
- [x] 4.4 Per-package security posture for every network-exposed package. *(Verified every network-exposed package's reference page against a security-relevant-keyword sweep: `cors`, `helmet`, `csrf`, `rate-limit`, `cookies`, `body-parser`, `multipart`, `validation`, `static`, `openapi`, `websocket`, `template`, `logger`, `request-id`, `compression` all already had adequate posture coverage. Two genuine gaps found and closed: `@nextrush/health` (no posture note — added: unauthenticated by default, check-name disclosure, rate-limit exemption) and `@nextrush/timer` (no posture note — added: response-timing side-channel risk on sensitive routes). Both added to `production/security.mdx`'s checklist and the `health` reference page.)*
- [x] 4.5 Roadmap treatment for unbuilt Phase 3 features (OTel/metrics/config/auth/session): labeled planned / not-yet-available, no runnable examples. Note the T064 dependency for audit-driven hardening guidance. *(Verified: `production/observability/request-tracing.mdx` explicitly states NextRush ships no `@nextrush/metrics`/`@nextrush/tracing` package and frames OpenTelemetry as a bring-your-own pattern, not a shipped feature. Also verified `Config`/`getConfigPrefix` in `@nextrush/di` are a real, shipped DI-namespacing feature — not the unbuilt T035 typed-config enterprise feature — so no D4 violation there either. No Phase 3 feature found documented as usable.)*

## 5. Verify and close

- [x] 5.1 `pnpm docs:validate:strict` green. *(Confirmed: 134 files scanned, 0 errors, 0 warnings, after all edits in this pass.)*
- [x] 5.2 Public-surface-coverage check: no locked export lacks a reference entry. *(Performed a ground-truth symbol extraction from all 34 `public-surface.test.ts` files (the actual T005 lock files) and cross-checked each package's expected export list against its reference page. Found and closed 3 real gaps: `@nextrush/openapi`'s `generateDocument`/`toOpenApiPath`/`extractPathParams` were exported but had no reference entry — added a "Programmatic API" section. `@nextrush/logger`'s ~50-symbol `@nextrush/log` re-export surface (config, transports, formatters, context, redaction, levels) had no reference entry — added 6 compact TypeTable sections. `@nextrush/cors` and `@nextrush/helmet`'s explicitly-labeled "advanced/custom-implementation" utility exports (origin validation, header helpers, CSP/nonce/Permissions-Policy builders, validation primitives) had no reference entry — added "Advanced" sections to both. Residual, knowingly-undocumented-in-full: `@nextrush/template`, `@nextrush/rate-limit`, `@nextrush/cookies`, `@nextrush/body-parser` each export a further set of internal constants/helpers (e.g. `SIZE_UNITS`, `IPV4_MAX_OCTET`, `COOKIE_PREFIXES`, parsing regexes) that back their already-documented primary middleware but were not individually tabled — a judgment call under D2's Tier-2 depth convention rather than a fabricated "fully covered" claim; flagging honestly rather than either padding low-value internal-constant tables or falsely marking this item complete.)*
- [x] 5.3 Human accuracy spot-check per the `engineering-documentation` review checklist (EDS-014 / EDS-015). *(Ran EDS-015 publish-safety checks against all pages authored/edited this pass: no TODO/FIXME/placeholder markers, all internal links resolve to real pages, all new pages are reachable via their `meta.json` nav — none orphaned. Ran EDS-014 substance checks (purpose, hook, technical accuracy, production-readiness coverage) as a structural review against the new `health.mdx`, `testing.mdx`, and `serverless.mdx` pages. Did not run a full EDS-014 pass against all 134 pre-existing files — that would be a separate, larger documentation-quality audit beyond this task's accuracy/coverage scope.)*
- [x] 5.4 Confirm no unbuilt feature is documented as usable (D4 scope guard holds). *(Verified — see 4.5.)*
- [x] 5.5 Update gap-checklist glyph T058 (□ → ☑) with a Verified note; record that T060 still requires T064 (security audit).
- [x] 5.6 `openspec validate phase5-complete-user-docs --strict` passes. *(Confirmed: "Change 'phase5-complete-user-docs' is valid".)*
