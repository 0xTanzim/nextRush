/**
 * @nextrush/di - Reflection Utilities
 *
 * Centralized Reflect.* API usage for di package.
 * This is the single source of metadata access across the di module.
 */

/**
 * Placeholder module for future centralized Reflect API usage.
 *
 * Currently, Reflect.* calls are distributed across service-decorators.ts,
 * service-metadata.ts, and injection.ts for logical cohesion with their
 * respective metadata writes/reads. This module exists to anchor the pattern
 * and can consolidate deeper reflection utilities if needed.
 *
 * Exported for completeness; not yet used.
 *
 * @internal
 */

export const REFLECTION_ANCHORED = true;
