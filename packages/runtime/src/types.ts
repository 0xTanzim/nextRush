/**
 * @nextrush/runtime - Type Definitions
 *
 * Re-exports core types from @nextrush/types for convenience.
 *
 * @packageDocumentation
 */

// Re-export types from @nextrush/types
// These are the canonical type definitions
export type {
    BodySource,
    BodySourceOptions,
    Runtime,
    RuntimeCapabilities,
    RuntimeInfo
} from '@nextrush/types';
