---
title: '[PLACEHOLDER: Concept name — e.g. "Middleware", "Dependency Injection"]'
description: '[PLACEHOLDER: One sentence — what this concept is and the problem it solves. Shown in search results and card grids.]'
---

<!--
CONCEPT PAGE TEMPLATE
Tier: matches docs-standards.instructions.md Tier 1 (core infrastructure) or a lighter
cut of it for a single sub-concept. Word budget for a full Tier 1 page: 1200-2000 words.
Audience: developers who have never used NextRush. Explain WHY before HOW.
Delete this comment block before publishing.
-->

## The Real Problem

[PLACEHOLDER: State the problem developers already feel, in their terms — not
NextRush's terms. What breaks, what's tedious, or what's error-prone without this
concept? 2-4 sentences. Do not mention NextRush's API yet.]

## Why NextRush Solves It This Way

[PLACEHOLDER: Explain the design decision and its rationale. What alternative
approaches exist (in other frameworks or in raw Node.js) and why NextRush chose
this one. Name the trade-off explicitly — every design choice costs something.]

<Callout type="info" title="[PLACEHOLDER: disambiguation title, or delete this callout]">
  [PLACEHOLDER: One version-specific note, default behavior clarification, or
  good-to-know fact. 1-3 sentences. Delete if not needed — do not pad.]
</Callout>

## Mental Model

[PLACEHOLDER: Give the reader one mental image or metaphor they can hold onto.
A diagram is encouraged for Tier 1 pages — use Mermaid for flows/architecture,
max 2 per page, under 10 nodes, real component names from source.]

<Mermaid
  chart="
flowchart LR
    A[PLACEHOLDER: step 1] --> B[PLACEHOLDER: step 2]
    B --> C[PLACEHOLDER: step 3]
"
/>

[PLACEHOLDER: Always explain the diagram in text immediately after it — never
leave a diagram to speak for itself.]

## How It Works

[PLACEHOLDER: The execution flow / lifecycle, in prose. What happens, in what
order, and what NextRush guarantees at each step. This is still conceptual —
save exhaustive signatures for the Reference page and link to it instead of
duplicating.]

## Minimal Correct Usage

```ts title="[PLACEHOLDER: filename, e.g. app.ts]"
import { createApp, listen } from 'nextrush';

// [PLACEHOLDER: minimal, runnable example — copy-paste correct, imports included,
// demonstrates exactly one concept]
const app = createApp();

listen(app, 8080);
```

[PLACEHOLDER: One or two sentences on what this example demonstrates and why it's
the minimal correct shape — not a line-by-line narration of the syntax.]

## What Happens Automatically

[PLACEHOLDER: List the zero-config default behavior. Per docs-standards.instructions.md,
"zero config ≠ zero explanation" — every automatic behavior needs its trade-off and
escape hatch stated, even briefly.]

- [PLACEHOLDER: automatic behavior 1 — and its escape hatch]
- [PLACEHOLDER: automatic behavior 2 — and its escape hatch]

## Trade-offs & Guarantees

[PLACEHOLDER: What does NextRush explicitly guarantee here (ordering, immutability,
error propagation, performance characteristic)? What does it explicitly NOT guarantee?
Be honest about limitations — this section is required, not optional decoration.]

<Callout type="warn" title="[PLACEHOLDER: non-obvious behavior title, or delete]">
  [PLACEHOLDER: A common misunderstanding or edge case that isn't obvious from the
  happy-path example above. Delete if the concept has none.]
</Callout>

## Common Mistakes

[PLACEHOLDER: Document 2-4 real mistakes — not hypothetical ones. Each entry:
what the mistake looks like, why it happens, the fix.]

1. **[PLACEHOLDER: mistake name]** — [PLACEHOLDER: what goes wrong and the fix].
2. **[PLACEHOLDER: mistake name]** — [PLACEHOLDER: what goes wrong and the fix].

## When Not to Use This

[PLACEHOLDER: Name the scenario where this concept is the wrong tool, or where a
simpler alternative already in NextRush solves the problem better. Every concept
page must include this — it is what makes the page honest rather than promotional.]

## Related

- [PLACEHOLDER: link to a Guide page that uses this concept in a task]
- [PLACEHOLDER: link to the Reference page with full signatures]
- [PLACEHOLDER: link to a sibling Concept page]
