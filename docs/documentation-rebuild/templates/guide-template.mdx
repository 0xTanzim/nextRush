---
title: '[PLACEHOLDER: Task-oriented title — e.g. "Add JWT Authentication"]'
description: '[PLACEHOLDER: One sentence naming the task this guide completes.]'
---

<!--
GUIDE PAGE TEMPLATE
Content ownership: guides/ = TASKS only. Do not re-explain concepts here — link to
the concept page. A guide must be step-by-step and end with a verification step.
Delete this comment block before publishing.
-->

[PLACEHOLDER: One paragraph stating the task and who needs it. No abstract
explanation — guides solve a specific task, not a category of tasks.]

## What You Will Build

[PLACEHOLDER: Describe the concrete end state — e.g. "an endpoint that issues and
verifies JWTs" — not the underlying concept. 1-3 sentences.]

## Prerequisites

- [PLACEHOLDER: e.g. NextRush app scaffolded — link to Quick Start]
- [PLACEHOLDER: package(s) this guide requires, installed via the Steps below]
- [PLACEHOLDER: any assumed prior guide/concept — link it, do not restate it]

## Steps

<Steps>
  <Step>
    ### [PLACEHOLDER: first action, e.g. "Install the package"]

    ```bash
    pnpm add [PLACEHOLDER: @nextrush/package-name]
    ```

    [PLACEHOLDER: one sentence on what this installs, if not obvious.]

  </Step>
  <Step>
    ### [PLACEHOLDER: second action]

    ```ts title="[PLACEHOLDER: filename]"
    import { createApp, listen } from 'nextrush';

    // [PLACEHOLDER: runnable code for this step]
    ```

    [PLACEHOLDER: why this step exists — the side effect or guarantee it sets up.]

  </Step>
  <Step>
    ### [PLACEHOLDER: third action]

    ```ts title="[PLACEHOLDER: filename]"
    // [PLACEHOLDER: continuation — builds directly on the previous step's code]
    ```

  </Step>
</Steps>

<Callout type="warn" title="[PLACEHOLDER: a common mistake at this exact task, or delete]">
  [PLACEHOLDER: 1-3 sentences on the mistake and how to avoid it. Delete if none.]
</Callout>

## Verification

[PLACEHOLDER: A concrete, runnable check that proves the task worked — a curl
command, a test snippet, or an expected response. Every guide must be verifiable;
this is not optional.]

```bash
[PLACEHOLDER: e.g. curl -X POST http://localhost:8080/login -d '...']
```

**Expected result:** [PLACEHOLDER: what the reader should see if it worked.]

## Next Improvements

[PLACEHOLDER: Optional. 1-3 follow-up steps a reader might take next — link to
guides, recipes, or production docs. Delete this section if there is nothing
concrete to add; do not pad with generic "explore more" language.]

## Related

- [PLACEHOLDER: link to the concept page this guide relies on]
- [PLACEHOLDER: link to the reference page for the package used]
