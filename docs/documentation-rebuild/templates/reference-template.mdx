---
title: '[PLACEHOLDER: API surface name — e.g. "ctx.json()", "createApp()", "@nextrush/cors"]'
description: '[PLACEHOLDER: One sentence — what this API does. Lookup material, not teaching material.]'
---

<!--
REFERENCE PAGE TEMPLATE
Lookup material only — optimized for scanning, not narrative. Do not explain
concepts here; link to the concept/guide page instead. Every statement must be
verified against actual source code before publishing — outdated reference docs
are treated as bugs (docs-standards.instructions.md).

Reference pages are the ONE place granular @nextrush/* subpackage imports are shown.
Concept/Guide/Tutorial pages teach with `nextrush` / `nextrush/class` imports only.
Delete this comment block before publishing.
-->

### `[PLACEHOLDER: functionName()]`

[PLACEHOLDER: one sentence — what it does and when to use it.]

**Signature:**

```ts
function [PLACEHOLDER: functionName](param1: [PLACEHOLDER: ParamType], options?: [PLACEHOLDER: OptionsType]): [PLACEHOLDER: ReturnType];
```

<TypeTable
  title="Parameters"
  types={{
    param1: { type: '[PLACEHOLDER: ParamType]', description: '[PLACEHOLDER: what this parameter controls]' },
    options: { type: '[PLACEHOLDER: OptionsType]', description: '[PLACEHOLDER: configuration object]', optional: true },
  }}
/>

<TypeTable
  title="Options"
  types={{
    '[PLACEHOLDER: optionName]': { type: '[PLACEHOLDER: type]', description: '[PLACEHOLDER: description]', default: '[PLACEHOLDER: default value]' },
  }}
/>

**Returns:** `[PLACEHOLDER: ReturnType]` — [PLACEHOLDER: description].

**Throws:**

| Error | Condition |
| ----- | --------- |
| `[PLACEHOLDER: ErrorClass]` | [PLACEHOLDER: when it's thrown] |

<!-- Delete the Throws table entirely if this API never throws. -->

**Example:**

```ts
import { [PLACEHOLDER: functionName] } from '@nextrush/[PLACEHOLDER: package-name]';

const result = [PLACEHOLDER: functionName]('[PLACEHOLDER: value]', { /* [PLACEHOLDER: options] */ });
```

<Callout type="warn" title="Deprecated">
  [PLACEHOLDER: only include this callout if the API is deprecated — state the
  removal version and the replacement API. Delete this callout entirely otherwise.]
</Callout>

---

<!-- Repeat the block above (### heading through Example) for each additional
     exported function/method/property on this page. Keep one page per capability
     cluster, not one page per single export, unless the cluster has only one export. -->

## Related

- [PLACEHOLDER: link to the concept page explaining why this API exists]
- [PLACEHOLDER: link to a guide that uses this API in a task]
