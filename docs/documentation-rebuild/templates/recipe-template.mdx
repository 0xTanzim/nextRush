---
title: '[PLACEHOLDER: Recipe title — e.g. "Paginate a List Endpoint"]'
description: '[PLACEHOLDER: One sentence — the specific, narrow problem this recipe solves.]'
---

<!--
RECIPE PAGE TEMPLATE
Lives under recipes/ — the cookbook (PLAN.md T16). A recipe is SHORTER and
NARROWER than a Guide: one copy-paste solution to one specific problem, minimal
prose, no multi-step Steps flow. Every recipe must link to the concept page it
draws on. Delete this comment block before publishing.
-->

[PLACEHOLDER: One or two sentences framing the specific problem — narrower than a
guide's task, e.g. "cursor-based pagination for a list endpoint" not "building an
API." Skip background — the reader already knows why they're here.]

## Solution

```ts title="[PLACEHOLDER: filename]"
import { createApp, listen } from 'nextrush';

// [PLACEHOLDER: complete, copy-paste-runnable solution — no partial snippets]
```

[PLACEHOLDER: One short paragraph, max 3 sentences, on the one non-obvious part of
the solution. Do not re-explain the concept — link to it instead.]

<Callout type="info" title="[PLACEHOLDER: variation or edge case, or delete]">
  [PLACEHOLDER: a common variation of this recipe — e.g. a different data source
  — in 1-3 sentences. Delete if the recipe has no notable variation.]
</Callout>

## Try It

```bash
[PLACEHOLDER: curl or CLI command proving the recipe works]
```

**Expected result:** [PLACEHOLDER: exact expected output.]

## Related

- [PLACEHOLDER: link to the concept page this recipe is built on]
- [PLACEHOLDER: link to a related recipe, if one exists]
