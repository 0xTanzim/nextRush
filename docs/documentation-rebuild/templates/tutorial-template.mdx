---
title: '[PLACEHOLDER: Tutorial title — e.g. "Build Your First NextRush API"]'
description: '[PLACEHOLDER: One sentence — the end state the reader reaches by the end.]'
---

<!--
TUTORIAL PAGE TEMPLATE
Lives under start/ — the onboarding spine. Unlike a Guide (one task, assumes an
existing app), a Tutorial takes a reader from zero to a running result, teaching
concepts along the way in the order they're needed. Teach with `nextrush` /
`nextrush/class` imports only — granular @nextrush/* imports belong in Reference.
Delete this comment block before publishing.
-->

[PLACEHOLDER: One paragraph — what the reader builds and why it's the right first
thing to build. Set expectations: time to complete, what they'll have at the end.]

<Callout type="info" title="Before you start">
  [PLACEHOLDER: prerequisites in one sentence — e.g. Node.js version, package
  manager. Link to an installation page rather than repeating install steps here.]
</Callout>

## What You Will Build

[PLACEHOLDER: Concrete description of the finished result — e.g. "a JSON API with
two routes and one middleware." Screenshots/output samples welcome if applicable.]

## Prerequisites

- [PLACEHOLDER: e.g. Node.js ≥22 installed]
- [PLACEHOLDER: e.g. a terminal and an editor]

<Steps>
  <Step>
    ### [PLACEHOLDER: Create the project]

    ```bash
    [PLACEHOLDER: pnpm create nextrush my-api]
    ```

    [PLACEHOLDER: what this generates, in one or two sentences.]

  </Step>
  <Step>
    ### [PLACEHOLDER: Write the first route]

    ```ts title="src/index.ts"
    import { createApp, listen } from 'nextrush';

    const app = createApp();

    // [PLACEHOLDER: minimal handler introducing one new concept at a time]
    app.get('/', (ctx) => ctx.json({ message: 'Hello NextRush!' }));

    listen(app, 8080);
    ```

    [PLACEHOLDER: explain the ONE new concept this step introduces — e.g. what
    `ctx.json` guarantees. Link to the concept page instead of re-teaching it fully.]

  </Step>
  <Step>
    ### [PLACEHOLDER: Run it]

    ```bash
    [PLACEHOLDER: pnpm dev]
    ```

    [PLACEHOLDER: what the reader should see in the terminal.]

  </Step>
  <Step>
    ### [PLACEHOLDER: Verify it]

    ```bash
    [PLACEHOLDER: curl http://localhost:8080/]
    ```

    **Expected output:**

    ```json
    [PLACEHOLDER: exact expected response]
    ```

  </Step>
</Steps>

## What Happened

[PLACEHOLDER: A short recap connecting the steps to the underlying model — e.g.
"the request flowed through the middleware pipeline to the route handler." This
is the moment to link out to the relevant concept page(s) for depth, not to
re-explain them in full.]

## Next Steps

- [PLACEHOLDER: link to the next tutorial in the onboarding spine]
- [PLACEHOLDER: link to a concept page for depth]
- [PLACEHOLDER: link to a guide for the next real task]
