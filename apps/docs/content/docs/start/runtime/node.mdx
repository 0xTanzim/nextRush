---
title: Node.js
description: Install NextRush on Node.js, run a hello-world server, and see how the Node adapter sets the baseline every other runtime is measured against.
difficulty: beginner
estimatedTime: 10 minutes
prerequisites: []
---

<DocHero eyebrow="Runtime · Baseline">

Node.js is the reference runtime — the one every capability, every middleware package, and every
adapter contract is built and tested against first. If you're not sure which runtime to start
with, this is it: maximum compatibility, the largest ecosystem, and every doc example assumes it
unless stated otherwise. The tradeoff is the traditional one — a long-running process instead of
a stateless request, and a heavier footprint than Bun or the edge.

<div className="not-prose mt-5 flex flex-wrap gap-2">
  <DocHeroPill>⭐ Recommended for beginners</DocHeroPill>
  <DocHeroPill>~10 min</DocHeroPill>
  <DocHeroPill>Beginner</DocHeroPill>
</div>

</DocHero>

<RuntimeSupport
  support={{ node: 'full', bun: 'full', deno: 'full', edge: 'partial', serverless: 'partial' }}
  links={{ bun: '/docs/start/runtime/bun', deno: '/docs/start/runtime/deno', edge: '/docs/start/runtime/edge', serverless: '/docs/start/runtime/serverless' }}
/>

"Partial" means the adapter is Stable but not yet verified against the real platform in CI (see
the [compatibility matrix](/docs/help/compatibility-matrix)) — not that it's missing features.

Targeting a different runtime? [Bun](/docs/start/runtime/bun) ·
[Deno](/docs/start/runtime/deno) · [Edge](/docs/start/runtime/edge) ·
[Serverless](/docs/start/runtime/serverless) each implement the same Context and Application
contract. Not sure which one fits? See the [runtime decision
guide](/docs/start/runtime/decision-guide).

## Finished project

By the end you'll have a server on port `8080` answering one route, and a clear picture of what
the Node adapter provides as the baseline every other runtime is measured against:

<DocStatStrip>
  <DocStat label="Runtime" value="Node.js" hint="Plain node:http — no build step" />
  <DocStat label="Outcome" value="1 route" hint="Answers JSON on port 8080" />
</DocStatStrip>

```bash
curl http://localhost:8080/
# → {"runtime":"node","status":"ok"}
```

## Prerequisites

- **Required:** a terminal and a package manager (npm, pnpm, or yarn). No prior NextRush
  knowledge assumed.
- **Difficulty · time:** Beginner · ~10 minutes

<Steps>

### Step 1 of 4 — Confirm your Node.js version

**Why now:** NextRush targets **Node.js 22 or later** — the engine floor the framework's ESM-only
policy and `node:*` APIs assume. Checking first avoids a confusing failure later.

**Do:**

```bash
node --version
# needs to read v22.x.x or higher
```

If you're on an older line, install 22+ with [nvm](https://github.com/nvm-sh/nvm) or from
[nodejs.org](https://nodejs.org/) before continuing.

**Under the hood:** the Node adapter (`@nextrush/adapter-node`) is built against Node 22's stable
ESM loader and `node:http` surface. An older runtime is not a supported target, not a "mostly
works" one.

### Step 2 of 4 — Install NextRush

**Why now:** the `nextrush` meta-package already includes the Node adapter, so one install is
everything this page needs.

**Do:**

<PackageInstall packages={["nextrush"]} dev={["tsx", "typescript", "@types/node"]} />

Or scaffold a project instead of adding it by hand:

```bash
pnpm create nextrush my-api --runtime node
```

**Why it worked:** `@nextrush/core`, `@nextrush/router`, and `@nextrush/adapter-node` are all
re-exported through the `nextrush` package, so `createApp`, `createRouter`, and `listen` are
available from one import without adding the adapter separately. On every other runtime NextRush
supports (Bun, Deno, edge, serverless), that same `nextrush` import stays identical — only the
adapter running underneath changes.

### Step 3 of 4 — Run a hello-world server

**Do:** create `src/index.ts`:

```ts title="src/index.ts"
import { createApp, createRouter, listen } from 'nextrush';

const app = createApp();
const router = createRouter();

router.get('/', (ctx) => {
  ctx.json({ runtime: 'node', status: 'ok' });
});

app.route('/', router);
await listen(app, 8080);
```

Run it:

```bash
npx tsx src/index.ts
```

**Expected output:**

```bash
curl http://localhost:8080/
# → {"runtime":"node","status":"ok"}
```

**Why it worked:** `listen(app, 8080)` calls into `@nextrush/adapter-node`, which starts a plain
`node:http` server and hands each incoming request to NextRush's router through the Context API.
Nothing here is Node-specific in your code — the same four calls (`createApp`, `createRouter`,
route registration, `listen`) are what every runtime's onboarding page starts from.

### Step 4 of 4 — Know the baseline

**What Node gives you, and why:** Node is the full-capability baseline the framework is designed
against — full `node:*` module access, no cold-start behavior to design around, and every
middleware package (`@nextrush/body-parser`, `@nextrush/static`, `@nextrush/multipart`, …) targets
it directly. There is no Node-specific limitation to work around here; runtimes further along the
persona journey (edge workers without `node:*`, serverless with cold starts) are the ones that
trade capability for reach, not Node.

<Callout type="warn">
This does not mean Node is required — Bun, Deno, edge, and serverless adapters all implement the
same Context and Application contract (see [each adapter's
reference](/docs/reference/platforms)). It means Node is where that contract has the fewest
constraints, so it's the reference point the [runtime compatibility
concept](/docs/concepts/runtime-compatibility) measures every other adapter against.
</Callout>

</Steps>

## Checkpoint

Confirm the server answers before moving on — if your output matches this, you're ready for what's
next:

```bash
curl -i http://localhost:8080/
# → HTTP/1.1 200 OK
# → {"runtime":"node","status":"ok"}
```

Your project now looks like:

```text
my-api/
  package.json
  src/
    index.ts
```

**🎉 That's a real NextRush server.** Every capability the framework ships targets this runtime
first — you're not on a simplified demo path.

## What you learned

Three things now describe how a request reaches your code on Node.js:

- **Node is the baseline runtime** — the framework's `node:*`-using packages, the Node adapter,
  and the docs' default code examples all target it first. [Runtime compatibility
  concept](/docs/concepts/runtime-compatibility)
- **One `nextrush` import covers the adapter** — `createApp`, `createRouter`, and `listen` all
  come from the meta-package; you never import `@nextrush/adapter-node` directly unless you need
  its lower-level `serve()`/`createHandler()` surface.
- **The same code moves between runtimes** — nothing in `src/index.ts` above is Node-specific;
  swapping to Bun changes the runtime underneath, not your route handlers.

## Next steps

<Callout type="info" title="🚀 Build a Task API — recommended · ~20 min">
  Go past hello-world on the runtime you set up: routing, a JSON body, and a real 404.
  [Start the tutorial →](/docs/start/quick-start)
</Callout>

Other guides:

<Cards>
  <Card title="Deploy to production" href="/docs/production/deployment/node">
    Docker, PM2, and systemd deployment paths for a Node.js NextRush server.
  </Card>
  <Card title="Node adapter reference" href="/docs/reference/platforms/node">
    The full `@nextrush/adapter-node` surface — `serve()`, `createHandler()`, and Node-specific
    Context utilities.
  </Card>
</Cards>

## Continue learning

<Cards>
  <Card title="Node adapter reference" href="/docs/reference/platforms/node">
    The full `@nextrush/adapter-node` surface — `serve()`, `createHandler()`, and Node-specific
    Context utilities.
  </Card>
  <Card title="Runs on any runtime" href="/docs/concepts/runtime-compatibility">
    Why Node, Bun, Deno, and the edge all run identical application code.
  </Card>
  <Card title="Try Bun next" href="/docs/start/runtime/bun">
    Same API, `Bun.serve()` underneath — the fastest way to feel the one import that changes.
  </Card>
</Cards>
