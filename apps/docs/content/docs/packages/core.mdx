---
title: Core Packages
description: The essential packages that form NextRush's foundation
---

# Core Packages

The packages that form NextRush's foundation — framework, DI, error handling, and logging.

<PackageGrid>

<PackageSection icon="⚡" title="Core Packages">
  <PackageCard name="nextrush" description="Core framework with routing, middleware, and HTTP server" essential install="pnpm add nextrush" />
  <PackageCard name="create-nextrush" description="CLI scaffolding to create new projects" install="pnpm create nextrush" />
  <PackageCard name="@nextrush/core" description="Application, Context, Middleware primitives" essential install="pnpm add @nextrush/core" />
  <PackageCard name="@nextrush/di" description="Lightweight dependency injection container" install="pnpm add @nextrush/di" />
  <PackageCard name="@nextrush/errors" description="Standardized error handling" essential install="pnpm add @nextrush/errors" />
  <PackageCard name="@nextrush/log" description="Universal, zero-dependency logger" install="pnpm add @nextrush/log" />
</PackageSection>

</PackageGrid>
