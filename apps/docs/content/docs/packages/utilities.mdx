---
title: Utilities
description: Middleware, security, compression, and dev tooling
---

# Utilities

Middleware packages for security, parsing, compression, observability, and development tooling.

<PackageGrid>

<PackageSection icon="🛡️" title="Middleware & Utilities">
  <PackageCard name="@nextrush/body-parser" description="Body parsing (JSON, URL-encoded, raw)" essential install="pnpm add @nextrush/body-parser" />
  <PackageCard name="@nextrush/cookies" description="Cookie parsing and serialization" install="pnpm add @nextrush/cookies" />
  <PackageCard name="@nextrush/cors" description="Cross-Origin Resource Sharing" essential install="pnpm add @nextrush/cors" />
  <PackageCard name="@nextrush/csrf" description="CSRF protection with HMAC-SHA256" install="pnpm add @nextrush/csrf" />
  <PackageCard name="@nextrush/helmet" description="Security headers middleware" essential install="pnpm add @nextrush/helmet" />
  <PackageCard name="@nextrush/rate-limit" description="Production-grade rate limiting" essential install="pnpm add @nextrush/rate-limit" />
  <PackageCard name="@nextrush/compression" description="Response compression (Gzip, Brotli, Deflate)" install="pnpm add @nextrush/compression" />
  <PackageCard name="@nextrush/multipart" description="Multipart form-data and file uploads" install="pnpm add @nextrush/multipart" />
  <PackageCard name="@nextrush/request-id" description="Request ID for distributed tracing" install="pnpm add @nextrush/request-id" />
  <PackageCard name="@nextrush/timer" description="Response time measurement" install="pnpm add @nextrush/timer" />
  <PackageCard name="@nextrush/dev" description="Dev server and build tools with multi-runtime support" install="pnpm add -D @nextrush/dev" />
</PackageSection>

</PackageGrid>
